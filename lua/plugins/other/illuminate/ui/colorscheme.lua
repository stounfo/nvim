local colors = require("ui.colorscheme.colors")

return {
  IlluminatedWordText = { ctermbg = colors.normal_bg },
  IlluminatedWordRead = { ctermbg = colors.highlighted_bg },
  IlluminatedWordWrite = { ctermbg = colors.highlighted_bg },
}
